#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>

void main()
{
	int x, y;
	x = 0;
	y = 0;
	scanf("%d", &x);
	scanf("%d", &y);
	if (x > y)
	{
		printf("x의 값이 y의 값보다 큽니다. \n");
	}
	else (x < y);
	{
		printf("x의 값이 y의 값보다 작습니다.");
	}

}